# Prisma uses Postgres only — NOT the anon/publishable keys on the API tab.
# Copy DATABASE_URL from Supabase: Connect (top) → Postgres → "Session pooler" if direct (db.*.supabase.co:5432) fails (IPv4 vs IPv6).
# Direct connection is IPv6-only; Session pooler works on most home networks.

DATABASE_URL="postgresql://postgres:ILoveAlishaKarma@db.dmrzcomlxwkauordjvcb.supabase.co:5432/postgres?sslmode=require"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
NODE_ENV="development"
NEXT_PUBLIC_HTTP_URL="http://localhost:3001"